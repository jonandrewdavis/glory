class_name PlayerHealthBar
extends Control
## Bottom-left readout of the local player's health.

const BAR_SIZE := Vector2(220, 16)
const MARGIN := 24.0
const FONT_SIZE := 12
const BACKING := Color(0.05, 0.06, 0.08, 0.85)
const OUTLINE := Color(0.03, 0.05, 0.09)
const FULL := Color(0.35, 0.8, 0.35)
const LOW := Color(0.85, 0.25, 0.2)
